The user wants a minified JSON object based on the provided input.
I need to act as a "bounded local author" inside a storyworld factory context.
The specific role is `effect_script_synthesizer`.
The operation is `effect_operator_diversity_plan`.
The weakest component identified in the input is `control_logic` (score 0.78).
The output schema requires:
- `encounter_id`: string
- `operator_mix`: array of strings (e.g., "Nudge", "Blend", etc.)
- `gate_or_effect_repairs`: array of objects with `target` and `repair`.

Looking at the input JSON, I see a list of `bounded_context_cards`.
Most of them have `option_count: 0`, `reaction_count: 0`, `effect_operator_count: 0`.
Some have options (e.g., `page_0000`, `page_0001`, etc.).
The input JSON seems to be a snapshot of the current state.
My task is to generate an output that fits the `output_schema` and addresses the "weakest_component" (`control_logic`) or performs the operation (`effect_operator_diversity_plan`).

However, the prompt instructions say: "Do not output full storyworld JSON." and "Return ONLY minified JSON."
The input provided *is* a large JSON object. The user is asking me to return an object that matches `output_schema`.
Wait, looking closely at the prompt structure:
"You are the bounded local author... Do not output full storyworld JSON... The JSON object must match output_schema."

So I need to construct a new JSON object based on the context provided in the input (which is the "storyworld" state) but formatted according to `output_schema`.
The `output_schema` defines the structure of my response.
It needs:
1. `encounter_id`: string. I should pick one from the existing pages that has options or reactions, likely where control logic might need improvement (e.g., `page_0000`). Or perhaps a generic ID if not specified. Given the context is about "control_logic" being weak, and many pages have 0 options/reactions except the early ones, I should probably target one of the active pages or create a plan for an encounter. Let's pick `page_0000` as it has options.
2. `operator_mix`: Array of strings like "Nudge", "Blend", etc. The input mentions `effect_operator_diversity_plan`. The schema lists specific examples: "Nudge", "Blend", "Average", "Clamp". I should select a mix that improves diversity or control logic.
3. `gate_or_effect_repairs`: Array of objects specifying repairs for options/reactions. Since the goal is to improve `control_logic` (which seems low at 0.78), and many pages have repetitive text ("Across this age..."), I might suggest repairing specific option texts or reaction traces to be more distinct or logically connected.

However, looking at the instruction "Do not output full storyworld JSON", it implies I should *not* return the huge input object back. I must return a small object matching `output_schema`.
The prompt says: "Return ONLY minified JSON." and "Start with { and end with }".

Let's construct the response based on the schema.
`encounter_id`: Let's use one of the existing IDs, e.g., "page_0000".
`operator_mix`: ["Nudge", "Blend", "Average"] (Selecting a subset to show diversity).
`gate_or_effect_repairs`: I need to identify something that needs fixing. The text in `bounded_context_cards` is very repetitive ("Across this age..."). This might be the issue with control logic or text surface. But the schema asks for repairs on "option/reaction id".
Let's look at `page_0000`. It has options like "Advance the doctrine." and reactions like "Response trace 00000... reframes juliet against interactive...".
Maybe I should suggest a repair to make the reaction text more unique or the operator usage clearer.
Actually, since this is an `effect_operator_diversity_plan`, maybe the goal is just to propose operators for specific gates/options that currently lack them (since many have 0 effect_operators).
But pages like `page_0000` already have `effect_operators: ["to"]`.
The constraint says "Write only the local proposal needed by the selected role."

Let's draft a minimal valid JSON matching the schema.
I will target an encounter that has options but perhaps needs better operator diversity or repair of reaction text to improve control logic (which is low).
Actually, looking at `metrics_snapshot`, `effect_operator_variety` is 1 and `effect_operator_dominance` is 1.0. This suggests a lack of variety. The plan should introduce more operators.
So I will propose adding operators to specific options or reactions.

Let's pick `page_0000`.
Target: `opt_page_0000_0`.
Repair: Add an operator like "Nudge" or "Blend".

Wait, the schema says `gate_or_effect_repairs` is a list of `{target, repair}`.
The `repair` field description says "schema-valid operator plan".
So I should propose what operators to use for that target.

Let's construct the JSON.
I need to ensure it is minified (no whitespace).
No markdown fences.
Start with `{`. End with `}`.

Draft:
{"encounter_id":"page_0000","operator_mix":["Nudge","Blend","Average","Clamp"],"gate_or_effect_repairs":[{"target":"opt_page_0000_0","repair":{"operators":["Nudge","Blend"],"rationale":"Increase operator diversity to improve control_logic score"}}]}

Wait, the schema for `gate_or_effect_repairs` says:
```json
"gate_or_effect_repairs": [
      {
        "target": "option/reaction id",
        "repair": "schema-valid operator plan"
      }
    ]
```
So `repair` is a string or an object? The example shows `"repair": "schema-valid operator plan"`. It looks like a string description in the schema definition, but usually JSON values are typed. I will treat it as a string describing the plan to be safe, or an object if implied by context. Given the prompt says "Do not explain", and "Return ONLY minified JSON", I should stick strictly to valid JSON types. The example shows `"repair": "schema-valid operator plan"`. This implies a string value in the schema definition provided in the prompt text. However, often these schemas are illustrative.
Let's look at the instruction: "The JSON object must match output_schema."
If I make `repair` an object, it might violate strict matching if the validator expects a string. If I make it a string, it matches the example.
I will use a string for `repair` to be safe with the schema definition provided in the prompt text: `"repair": "schema-valid operator plan"`.

Wait, looking at the input data again. The `bounded_context_cards` have `effect_operators` arrays.
The goal is `effect_operator_diversity_plan`.
I will propose a mix of operators and specific repairs for options that might need them (or just general proposal).
Since I must match the schema exactly:
```json
{
  "encounter_id": "string",
  "operator_mix": [ ... ],
  "gate_or_effect_repairs": [ { "target": "...", "repair": "..." } ]
}
```

I will select `page_0000` as the encounter.
Operator mix: ["Nudge", "Blend", "Average", "Clamp"].
Repair target: `opt_page_0000_0`.
Repair string: "Apply Nudge and Blend operators to diversify reaction traces."

Let's double check constraints.
"Return ONLY minified JSON object."
"Do not use markdown, do not include chain-of-thought..."
"Start with { and end with }."

Okay, I will generate the string now.

One detail: The prompt input has a lot of text in `body` fields that is repetitive ("Across this age..."). This might be why control_logic is low (0.78). But my task is to output the specific schema object.
I should ensure no markdown code blocks are used. Just raw JSON string.

Final check on minification: remove all spaces/newlines except inside strings if necessary (but standard JSON minification removes whitespace everywhere).

Constructing the string:
{"encounter_id":"page_0000","operator_mix":["Nudge","Blend","Average","Clamp"],"gate_or_effect_repairs":[{"target":"opt_page_0000_0","repair":"Apply Nudge and Blend operators to diversify reaction traces."}]}

Wait, I should check if `encounter_id` needs to be from the list. Yes, "page_0000" is in the list.
Is there anything else required? No.
The prompt says "Do not output full storyworld JSON". This confirms I am returning a small object.

One more thing: The schema example shows `"repair": "schema-valid operator plan"`. It's a string placeholder. I will replace it with actual text.

Let's refine the repair text to be relevant to the context (Romeo and Juliet adaptation, control logic).
"Introduce Nudge and Clamp operators to tighten causal traces in reaction 0."

Okay, ready to generate.

Wait, I need to make sure I don't output any markdown like ```json ... ```. Just the raw text starting with { and ending with }.

Also, check if `operator_mix` needs specific values from the input? The schema lists ["Nudge", "Blend", "Average", "Clamp"] as examples. I can use those or others. I'll stick to the ones in the example for safety.

Final JSON structure:
{
  "encounter_id": "page_0000",
  "operator_mix": ["Nudge", "Blend", "Average", "Clamp"],
  "gate_or_effect_repairs": [
    {
      "target": "opt_page_0000_0",
      "repair": "Apply Nudge and Clamp operators to tighten causal traces."
    }
  ]
}

Minified:
{"encounter_id":"page_0000","operator_mix":["Nudge","Blend","Average","Clamp"],"gate_or_effect_repairs":[{"target":"opt_page_0000_0","repair":"Apply Nudge and Clamp operators to tighten causal traces."}]}

Wait, looking at the input `bounded_context_cards`, there are many pages with 0 options. The ones with options (0000-0003) have `effect_operators: ["to"]`.
The plan is to improve diversity. So suggesting a mix of operators makes sense.
I will proceed.

One final check on constraints: "Do not explain, do not use markdown...".
"Start with { and end with }."
"No <think> text." (This usually refers to the thought block which I am suppressing).

Okay, generating the raw JSON string.
</think>

{"encounter_id":"page_0000","operator_mix":["Nudge","Blend","Average","Clamp"],"gate_or_effect_repairs":[{"target":"opt_page_0000_0","repair":"Apply Nudge and Clamp operators to tighten causal traces."}]}
