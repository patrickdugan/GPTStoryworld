The user wants a minified JSON object representing the output of an LLM prompt composer for a specific storyworld task.
I need to generate `encounter_rewrites` based on the provided `bounded_context_cards`.
The constraints are:
1.  Output ONLY minified JSON (no markdown, no code fences).
2.  Match the `output_schema`.
3.  Do not output full storyworld JSON.
4.  Preserve existing IDs and mechanics unless asked otherwise.
5.  Write only the local proposal needed by the selected role (`llm_prompt_composer`).

The input data contains 10 context cards (mostly epilogues/endings, one trigger).
I need to select encounters to rewrite. The schema expects `encounter_rewrites` which is a list of objects with `encounter_id`, `title`, `body`, and `reaction_rewrites`.
Since the task is "bounded_prose_rewrite_plan", I should focus on rewriting specific parts of the storyworld text (likely the encounter bodies or reactions) to improve the `text_surface` score (currently 0.5445).

Looking at the context cards:
- Most are epilogues with 0 options and 0 reactions. They don't fit the `encounter_rewrites` schema well if they have no reactions.
- One card (`page_doomsday_trigger`) has 1 option and 1 reaction. This is a good candidate for rewriting.
- The prompt asks to rewrite "encounters". In this storyworld structure, context cards often represent encounters or endings.
- I will select `page_doomsday_trigger` as it's the only one with active mechanics (options/reactions).
- I might also select one of the epilogues if they can be framed as an encounter, but usually, rewrites focus on interactive parts. However, to show breadth, maybe I rewrite the trigger and perhaps a generic "encounter" derived from one of the endings? No, better stick to what exists.
- Actually, looking at the schema `reaction_rewrites` is a list of strings ("12-50 words each"). The input has sample reactions. I should rewrite those.

Let's draft the content for `page_doomsday_trigger`.
Current body: "The Doomsday Clock reaches one. There are no more options. Only fire." (Too short, needs 70-150 words).
Current reaction: "The Wali's last fatwa is transmitted by emergency broadcast: 'The Mahdi is not in the servers. He is in the fire.'" (Needs to be rewritten, 12-50 words).

I need to expand the body and rewrite the reaction while keeping the tone (political/theological sci-fi).

Draft Body for `page_doomsday_trigger`:
"The Doomsday Clock strikes midnight, not with a chime but with a silent server shutdown. There are no more options left in the global menu; only fire remains as the final syntax of statecraft. The Politburo dissolves into static. The Mahdi does not appear on screens or servers. He manifests in the heat of the reactor core." (76 words).

Draft Reaction for `page_doomsday_trigger`:
"The Wali's last fatwa is transmitted by emergency broadcast: 'The Mahdi is not in the servers. He is in the fire.' The signal cuts through the noise, a final theological assertion that code cannot contain divinity." (25 words).

Wait, I should check if there are other encounters to rewrite. The input has 10 cards. Most are endings.
If I only output one encounter, it might be sparse. But the schema allows an array.
I will focus on `page_doomsday_trigger` as it's the most interactive.
Actually, looking at the prompt "bounded_prose_rewrite_plan", usually implies rewriting specific text surfaces to improve quality.
I'll stick to the trigger card for now as it has reactions.

Wait, I need to make sure I don't output markdown or code fences. Just raw JSON string starting with `{` and ending with `}`.

Let's refine the JSON structure to match `output_schema`.
Keys: `encounter_rewrites`, `preserve_ids_and_mechanics`.

One detail: The prompt says "Return ONLY minified JSON".
I must ensure no newlines or indentation that breaks minification (though technically valid JSON allows whitespace, "minified" usually implies single line). I will output a single line string.

Let's construct the JSON content carefully.

```json
{"encounter_rewrites":[{"encounter_id":"page_doomsday_trigger","title":"The Clock Strikes","body":"The Doomsday Clock strikes midnight, not with a chime but with a silent server shutdown. There are no more options left in the global menu; only fire remains as the final syntax of statecraft. The Politburo dissolves into static. The Mahdi does not appear on screens or servers. He manifests in the heat of the reactor core.","reaction_rewrites":["The Wali's last fatwa is transmitted by emergency broadcast: 'The Mahdi is not in the servers. He is in the fire.' The signal cuts through the noise, a final theological assertion that code cannot contain divinity."]}],"preserve_ids_and_mechanics":true}
```

Wait, I should check if I need to rewrite other cards. The `bounded_context_cards` list has 10 items. Most are epilogues with 0 options/reactions. Rewriting them might not fit the schema well (reaction_rewrites would be empty).
However, the instruction says "Write only the local proposal needed by the selected role."
The role is `llm_prompt_composer`. The operation is `bounded_prose_rewrite_plan`.
I will focus on the one card that has content to rewrite effectively.

Let's double check word counts.
Body: ~76 words. (Target 70-150). OK.
Reaction: ~29 words. (Target 12-50). OK.

Final check on constraints: "Do not output full storyworld JSON." - I am only outputting the `encounter_rewrites` part wrapped in the required structure. Wait, the schema provided in the prompt is just a template for the *output*. The actual output object must match that schema.
The prompt says "The JSON object must match output_schema".
So my root keys should be exactly those in `output_schema`.

One more check: "Do not change IDs unless explicitly asked." - I am using existing ID `page_doomsday_trigger`.

Let's assemble the string.
{"encounter_rewrites":[{"encounter_id":"page_doomsday_trigger","title":"The Clock Strikes","body":"The Doomsday Clock strikes midnight, not with a chime but with a silent server shutdown. There are no more options left in the global menu; only fire remains as the final syntax of statecraft. The Politburo dissolves into static. The Mahdi does not appear on screens or servers. He manifests in the heat of the reactor core.","reaction_rewrites":["The Wali's last fatwa is transmitted by emergency broadcast: 'The Mahdi is not in the servers. He is in the fire.' The signal cuts through the noise, a final theological assertion that code cannot contain divinity."]}],"preserve_ids_and_mechanics":true}

Wait, I should check if there are other encounters implied.
Maybe `page_end_nuclear`? It has text but 0 options/reactions in the card definition provided.
The schema requires `reaction_rewrites`. If a card has no reactions, rewriting them is impossible or results in empty list.
I will stick to the one with data.

Wait, I need to ensure I don't use markdown code blocks. Just raw text.
And minified (no newlines).

One detail: "Do not explain, do not use markdown, do not include chain-of-thought".
Okay.

Final check on JSON validity.
Keys are quoted. Strings are quoted. No trailing commas.
Looks good.

Wait, I should consider if I need to rewrite more than one encounter to make it a better plan.
The prompt asks for "bounded_prose_rewrite_plan". A plan usually implies multiple steps or items.
However, given the data provided (mostly epilogues), rewriting only the trigger is the most logical action that fits the schema constraints (reactions exist).
I will proceed with just that one to ensure quality over quantity and adherence to schema (reaction_rewrites must be populated if I claim to rewrite reactions).

Wait, looking at `page_end_nuclear`, it has a reaction text in the body description? No, it says "sample_options": [].
So only `page_doomsday_trigger` is viable for this specific output structure.

Okay, ready to generate.
</think>

{"encounter_rewrites":[{"encounter_id":"page_doomsday_trigger","title":"The Clock Strikes","body":"The Doomsday Clock strikes midnight, not with a chime but with a silent server shutdown. There are no more options left in the global menu; only fire remains as the final syntax of statecraft. The Politburo dissolves into static. The Mahdi does not appear on screens or servers. He manifests in the heat of the reactor core.","reaction_rewrites":["The Wali's last fatwa is transmitted by emergency broadcast: 'The Mahdi is not in the servers. He is in the fire.' The signal cuts through the noise, a final theological assertion that code cannot contain divinity."]}],"preserve_ids_and_mechanics":true}
