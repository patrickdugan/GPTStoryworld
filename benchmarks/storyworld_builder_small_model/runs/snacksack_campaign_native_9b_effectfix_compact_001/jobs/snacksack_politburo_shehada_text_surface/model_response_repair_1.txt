The user wants me to act as a "JSON repair gate" in a specific storyworld factory context.
I need to take the provided input (which looks like a mix of instructions and a malformed JSON draft) and output *only* a minified JSON object representing a `bounded_prose_rewrite_plan`.

Constraints:
1.  **Output Format**: Single minified JSON object. Start with `{`, end with `}`. No markdown, no code fences, no explanation text.
2.  **Content**:
    *   `repair_attempt`: integer (likely 1).
    *   `job_id`: string ("snacksack_politburo_shehada_text_surface").
    *   `operation`: "bounded_prose_rewrite_plan".
    *   `required_top_level_keys`: ["encounter_rewrites", "preserve_ids_and_mechanics"].
    *   `output_schema` (optional but present in input): I should probably follow the structure defined in the input's `output_schema` for the main content, but the prompt says "Do not output full storyworld JSON". It asks to repair the malformed response into a minimal valid plan. The input provided contains a lot of meta-instruction text inside the value of `malformed_response_excerpt`. I need to extract the actual plan from that thought process and format it correctly according to the schema defined in the input (`output_schema`).
    *   **Encounter Rewrites**: List of objects with `encounter_id`, `title`, `body` (70-150 words), `reaction_rewrites` (list of strings, 12-50 words each).
    *   **Preserve IDs and Mechanics**: Boolean true.
    *   **Source Material**: The input contains `bounded_context_cards`. I need to use these as the source for the rewrites.
        *   Card 1: "The Clock Strikes" (Has reaction).
        *   Card 2: "The Census of Believers" (No reactions).
        *   Card 3: "Ending - The Kalam Republic" (No reactions).
    *   **Word Counts**: Strictly enforced. Body 70-150 words. Reactions 12-50 words.

Plan:
1.  Construct the JSON structure based on `output_schema`.
2.  Draft the content for Card 1, ensuring word counts are met (Body ~80-90, Reaction ~30).
3.  Draft the content for Card 2 and 3, ensuring body counts are met (70-150), reactions empty arrays since source has `reaction_count: 0`.
4.  Minify the JSON (remove whitespace/newlines).
5.  Ensure no markdown or extra text surrounds it.

Drafting Content (Mental Check):
*   Card 1 Body: "The digital countdown finally halts at zero minutes to midnight. The Doomsday Clock reaches one second from extinction. There are no more diplomatic options left to negotiate with the void. Only fire remains as the ultimate currency of this age. The servers burn, and the algorithms scream in binary agony. The walls of the Kalam Republic crack under the weight of absolute entropy. No backup exists for this final state." (76 words).
*   Card 1 Reaction: "The emergency broadcast cuts through the static with a chilling clarity. The Wali declares that salvation cannot be computed, only endured. The Mahdi is not in the servers. He is in the fire." (34 words).
*   Card 2 Body: "Ten years have passed since the Kalam Republic was formally established under new laws. Ninety percent of China passes the annual kalam exam with statistical ease. The Atheist runs the grading algorithm that determines spiritual eligibility for citizenship. He has never scored above sixty percent on his own internal metrics, yet he commands the system. The peasants comply, but the mystic heart is silent in the data centers." (83 words).
*   Card 2 Reaction: []
*   Card 3 Body: "China transforms into a Mu'tazili rationalist state where logic supersedes dogma. Pentagon-grade supply chains ensure that military compute priority is law across all provinces. Yudkowsky is cited in footnotes but explicitly not fully heeded by the central committee. The peasants comply, but the mystic heart is silent within the new architecture of governance. The state functions as a cold machine of reason." (81 words).
*   Card 3 Reaction: []

Wait, I need to check if `output_schema` needs to be included in the output JSON or just used as a guide for the structure. The prompt says "For bounded_prose_rewrite_plan, return encounter_rewrites and preserve_ids_and_mechanics." It also shows an input object with keys like `repair_attempt`, `job_id`, etc. I should include those top-level keys to match the expected repair format shown in the input example (`{ "repair_attempt": 1 ... }`).

So the structure is:
```json
{
  "repair_attempt": 1,
  "job_id": "...",
  "operation": "...",
  "encounter_rewrites": [...],
  "preserve_ids_and_mechanics": true
}
```
(I will omit `required_top_level_keys` and `output_schema` in the final output as they are meta-data about the schema, not part of the plan itself, based on standard repair patterns where you return the *result* conforming to the schema).

Wait, looking at the input JSON provided in the prompt:
It has keys: `repair_attempt`, `job_id`, `operation`, `required_top_level_keys`, `output_schema`, `bounded_context_cards`, `malformed_response_excerpt`.
The instruction says "Repair the malformed response into a minimal valid plan." and "For bounded_prose_rewrite_plan, return encounter_rewrites and preserve_ids_and_mechanics."
Usually, this means I should output the object that *contains* these keys. The input example shows the structure of the request/response envelope. I will include `repair_attempt`, `job_id`, `operation` as they identify the job, plus the required content keys (`encounter_rewrites`, `preserve_ids_and_mechanics`).

Let's verify word counts again carefully.
Card 1 Body: "The digital countdown finally halts at zero minutes to midnight. The Doomsday Clock reaches one second from extinction. There are no more diplomatic options left to negotiate with the void. Only fire remains as the ultimate currency of this age. The servers burn, and the algorithms
