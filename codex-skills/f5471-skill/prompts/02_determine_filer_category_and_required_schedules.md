Determine categories and schedule set using only declared facts:

- ownership percentage
- CFC status flag
- tested income compute flag

Use deterministic rules in `config/filer_rules.yaml`. Do not produce legal analysis text.

